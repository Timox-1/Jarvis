name: Deploy to VPS (disabled)

# Public repo is a frozen hiring case. Live deploy: Timox-1/jarvis-os
on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy moved to private Timox-1/jarvis-os. This public repo is a frozen case snapshot (tag case-2026-08)."
